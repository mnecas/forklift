---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: toeholds.forklift.konveyor.io
spec:
  group: forklift.konveyor.io
  names:
    kind: Toehold
    listKind: ToeholdList
    plural: toeholds
    singular: toehold
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.stage
      name: STAGE
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ToeholdSpec defines the desired state of Toehold.
            properties:
              bootcImage:
                description: bootc container image to convert to a VMDK.
                type: string
              datastore:
                description: Target vCenter datastore.
                type: string
              disks:
                description: Migration disks to attach to the appliance and export
                  over NBD.
                items:
                  description: ToeholdDiskSpec describes a migration disk to attach
                    to the appliance and export over NBD.
                  properties:
                    capacityGiB:
                      description: Optional disk capacity in GiB when vCenter cannot
                        infer it from the source VM.
                      format: int64
                      type: integer
                    diskMode:
                      default: independent_nonpersistent
                      description: Disk mode for the attached disk.
                      type: string
                    name:
                      description: Logical name used in status and downstream CDI
                        references.
                      type: string
                    port:
                      description: NBD listen port on the appliance guest. Defaults
                        to nbd.basePort + disk index.
                      format: int32
                      type: integer
                    sourceFolder:
                      description: Optional folder for sourceVM when different from
                        spec.folder.
                      type: string
                    sourceVM:
                      description: Source VM inventory name.
                      type: string
                    vmdkPath:
                      description: Path to an existing VMDK backing file, e.g. [datastore1]
                        vm/vm-000001.vmdk.
                      type: string
                  required:
                  - name
                  - sourceVM
                  - vmdkPath
                  type: object
                type: array
              folder:
                description: vCenter folder path (e.g. /Datacenter/vm).
                type: string
              forceRebuild:
                description: Skip reuse checks and always rebuild the template and
                  reclone the VM.
                type: boolean
              images:
                description: Container image overrides.
                properties:
                  bootcImageBuilder:
                    description: bootc-image-builder initContainer image.
                    type: string
                  toeholdImporter:
                    description: toehold-importer main container image.
                    type: string
                type: object
              nbd:
                description: NBD export configuration for spec.disks.
                properties:
                  basePort:
                    default: 10809
                    description: Base port for the first disk export.
                    format: int32
                    type: integer
                  sshUser:
                    default: root
                    description: SSH user for guest access.
                    type: string
                  threads:
                    default: 8
                    description: nbdkit thread count per export.
                    format: int32
                    type: integer
                type: object
              network:
                description: Port group for OVF network mapping.
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: Optional node selector for the bib Job pod.
                type: object
              powerOn:
                default: true
                description: Power on the VM after clone.
                type: boolean
              provider:
                description: Reference to a vSphere Provider.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              registrySecret:
                description: Optional dockerconfigjson secret for private bootc image
                  pulls.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              resources:
                description: OVF hardware descriptor overrides.
                properties:
                  cpu:
                    default: 2
                    format: int32
                    type: integer
                  memoryMiB:
                    default: 4096
                    format: int32
                    type: integer
                type: object
              retainTemplate:
                default: true
                description: Keep the vCenter template on Toehold deletion.
                type: boolean
              targetNamespace:
                description: Namespace where the build Job runs. Defaults to the CR
                  namespace.
                type: string
              templateName:
                description: vCenter template name after OVF import.
                type: string
              vmName:
                description: Name of the cloned toehold VM.
                type: string
            required:
            - bootcImage
            - datastore
            - folder
            - network
            - provider
            - templateName
            - vmName
            type: object
          status:
            description: ToeholdStatus defines the observed state of Toehold.
            properties:
              completionTime:
                format: date-time
                type: string
              conditions:
                description: List of conditions.
                items:
                  description: Condition
                  properties:
                    category:
                      description: The condition category.
                      type: string
                    durable:
                      description: The condition is durable - never un-staged.
                      type: boolean
                    items:
                      description: A list of items referenced in the `Message`.
                      items:
                        type: string
                      type: array
                    lastTransitionTime:
                      description: When the last status transition occurred.
                      format: date-time
                      type: string
                    message:
                      description: The human readable description of the condition.
                      type: string
                    reason:
                      description: The reason for the condition or transition.
                      type: string
                    status:
                      description: The condition status [true,false].
                      type: string
                    suggestion:
                      description: A suggested action or resolution for the condition.
                      type: string
                    type:
                      description: The condition type.
                      type: string
                  required:
                  - category
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              job:
                description: Reference to the build Job when a rebuild ran.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              message:
                type: string
              nbd:
                description: NBD export endpoints when all disks are ready.
                items:
                  description: ToeholdNBDStatus tracks one attached disk and its NBD
                    export endpoint.
                  properties:
                    device:
                      description: Guest block device path, e.g. /dev/sdb.
                      type: string
                    message:
                      type: string
                    name:
                      description: Logical disk name from spec.
                      type: string
                    port:
                      description: NBD listen port on the appliance guest.
                      format: int32
                      type: integer
                    ready:
                      description: True when the export is attached and nbdkit is
                        listening.
                      type: boolean
                    sourceVM:
                      description: Source VM inventory name.
                      type: string
                    uri:
                      description: NBD URI clients should connect to, e.g. nbd://10.0.0.1:10809/.
                      type: string
                    vmdkPath:
                      description: Attached VMDK backing path.
                      type: string
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: ToeholdPhase is the high-level lifecycle state of a Toehold
                  resource.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              stage:
                description: ToeholdStage is the fine-grained pipeline position within
                  the Running phase.
                type: string
              template:
                description: ToeholdTemplateStatus tracks the vCenter template artifact.
                properties:
                  bootcImage:
                    description: bootcImage value at import time.
                    type: string
                  bootcImageID:
                    description: Resolved bootc image digest.
                    type: string
                  contentHash:
                    description: Content hash of the template artifact.
                    type: string
                  importedAt:
                    format: date-time
                    type: string
                  moref:
                    description: vSphere managed object reference.
                    type: string
                  reused:
                    description: True when an existing vCenter template was reused.
                    type: boolean
                type: object
              vm:
                description: ToeholdVMStatus tracks the cloned toehold VM.
                properties:
                  contentHash:
                    description: VM content hash.
                    type: string
                  ip:
                    description: Guest IP when available.
                    type: string
                  moref:
                    description: vSphere managed object reference.
                    type: string
                  reused:
                    description: True when an existing VM was reused.
                    type: boolean
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
